# Unix core utils

`wc, ls, cp, rm, mv, cat, head, tail, and sort`

## WC

- `wc ...path` -> word count output
  <br>i.e `25     60      500 src/main.rs` (num lines, num words, num bytes, filename)
- `wc -l ...path` -> outputs line count of a file
